# for production
version: '3.7'
services:
  mes-courses:
    image: abcdevelop/simple-breath:1.0.1
    container_name: simple-breath_1.0.1
    build:
      context: .
      dockerfile: Dockerfile
      target: 'production-stage'
    ports:
      - '8082:80'
    volumes:
      - '/app/node_modules'
      - '.:/app'
